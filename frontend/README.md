# Kaarigari Frontend

The `frontend` branch contains the customer marketplace and artisan-facing UI foundation.

The root `index.html` is the current mobile-first visual prototype. It includes customer catalog, role selection, OTP demo flow, artisan dashboard, voice input, image workflow and offline draft behavior.

## Local preview

Open `index.html` directly, or run any static server from the repository root.
